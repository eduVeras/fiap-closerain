version: 2.1

orbs:
  win: circleci/windows@1.0.0

jobs:
  build:
    docker:
      - image: microsoft/aspnetcore-build:2.2
    steps:
      - checkout
      - run: find .
      - run: dotnet restore
      - run: dotnet build
